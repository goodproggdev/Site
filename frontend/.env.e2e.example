# Copia in `.env.e2e` per sovrascrivere (file ignorato da git).
# Il build preview (`vite preview`, porta 4173) deve usare lo stesso backend del preflight.
E2E_API_BASE_URL=http://127.0.0.1:8000
E2E_TEST_EMAIL=demo.video@example.com
E2E_TEST_PASSWORD=DemoVideo2026!

# Imposta a 1 per saltare il test di login (es. pipeline senza API).
# E2E_SKIP_AUTH_LOGIN=1
